import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    padding: 24,
    alignItems: 'center',
  },
  appName: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 16,
    textAlign: 'center',
  },
  author: {
    fontSize: 16,
    marginBottom: 24,
    textAlign: 'center',
    color: '#666',
  },
  linksContainer: {
    width: '100%',
    gap: 12,
  },
  linkButton: {
    width: '100%',
  },
});
